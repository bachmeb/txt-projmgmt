Document takes an idea from Solicit and formalizes it. 

title (create)
description (create)
author (copy from solicit)
priority (create)
cost (create - this can only be a rough estimate at this point)
objective (create - this is cross-referenced with the group objectives/mission list)
status (create - default is 'new')
